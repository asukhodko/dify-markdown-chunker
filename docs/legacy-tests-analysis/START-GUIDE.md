# Руководство по использованию результатов анализа legacy-тестов

## Что было сделано

Проведён полный анализ всех тестов в проекте (475 тестов в 138 файлах) и создана комплексная документация для миграции на markdown_chunker_v2.

## Структура документации

Все результаты сохранены в **`docs/legacy-tests-analysis/`**:

### 📚 Основные документы

1. **README.md** - начните отсюда
   - Навигация по всей документации
   - Быстрые ссылки на нужные разделы
   - Индекс всех документов

2. **legacy-test-analysis.md** (42 КБ) - основной документ
   - **Полный реестр** всех 138 тестовых файлов
   - Категоризация по ценности и сложности миграции
   - Приоритеты (P0, P1, P2, P3)
   - План миграции по фазам

3. **test-migration-status.md** - текущее состояние
   - Статистика выполнения тестов
   - Метрики (475 тестов, 98 с ошибками импорта)
   - Список проблемных тестов

4. **p0-migration-tasks.md** - детальный план работы
   - 6 задач для немедленного выполнения
   - Детальные инструкции для каждого теста
   - Оценка времени (~4.5 часа на всё)

5. **MIGRATION_QUICK_REFERENCE.md** - шпаргалка
   - Готовые паттерны для copy-paste
   - Чеклист миграции
   - Решение типичных проблем

6. **IMPLEMENTATION_SUMMARY.md** - что сделано
   - Сводка по фазе 1
   - Метрики и статистика
   - Следующие шаги

## Как использовать реестр

### Категории тестов (ваше задание выполнено)

Все тесты разбиты на **5 категорий** (A-E):

#### **Категория A** - точно полезное, внедрить легко (P0)
- **28 файлов** (~320 тестов)
- Миграция: простое обновление импортов
- **Действие**: мигрировать НЕМЕДЛЕННО
- Примеры: property-based тесты, базовая функциональность

#### **Категория B** - точно полезное, но сложно (P1)
- **67 файлов** (~850 тестов)
- Требует рефакторинга логики теста
- **Действие**: запланировать на 2-3 спринта
- Примеры: тесты стратегий, парсера, метаданных

#### **Категория C** - вероятно полезное, легко (P2)
- **31 файл** (~360 тестов)
- Средняя ценность, простая миграция
- **Действие**: мигрировать при работе с релевантным кодом
- Примеры: edge cases, конфигурация, логирование

#### **Категория D** - вероятно полезное, сложно (P3)
- **6 файлов** (~80 тестов)
- Требует оценки после стабилизации v2
- **Действие**: оценить в фазе 4
- Примеры: legacy-специфичные поведения

#### **Категория E** - не мигрируется (None)
- **6 файлов** (~60 тестов)
- Тесты удалённых функций
- **Действие**: архивировать с документацией
- Примеры: mixed strategy, list strategy, table strategy (удалены в v2)

### Полный реестр тестов

Откройте **`legacy-test-analysis.md`** и найдите раздел **"Test Registry"**.

Там таблица для каждой категории с колонками:
- **Test File** - имя файла
- **Tests** - количество тестов
- **Primary Focus** - что тестирует
- **V2 Applicable** - степень полезности (Definitely/Probably/Unclear/Not)
- **Migration Effort** - сложность (Easy/Moderate/Difficult/Cannot)
- **Category** - категория (A/B/C/D/E)
- **Priority** - приоритет (P0/P1/P2/P3/None)
- **Notes** - важные замечания

## Практическое применение

### Сценарий 1: "Нужно запустить все важные тесты"

```bash
# Запустить P0-тесты (высший приоритет)
make test-p0

# Сейчас будет: 210 тестов загружаются, 6 с ошибками
# Цель: 320 тестов, все проходят
```

**Проблема**: 6 тестов не загружаются из-за legacy-импортов

**Решение**: Смотрите `p0-migration-tasks.md` - там 6 задач с детальными инструкциями

### Сценарий 2: "Нужно мигрировать конкретный тест"

1. Найдите тест в реестре (`legacy-test-analysis.md`)
2. Смотрите категорию и приоритет
3. Откройте `MIGRATION_QUICK_REFERENCE.md`
4. Используйте готовые паттерны:

```python
# БЫЛО (legacy)
from markdown_chunker.chunker.core import MarkdownChunker
from markdown_chunker.chunker.types import ChunkConfig

# СТАЛО (v2)
from markdown_chunker_v2 import MarkdownChunker, ChunkConfig
```

### Сценарий 3: "С чего начать миграцию?"

**Фаза 1** (сейчас): Мигрируйте 6 проблемных P0-тестов

Откройте `p0-migration-tasks.md`:

1. **test_monotonic_ordering_property.py** - 30 мин
2. **test_no_empty_chunks_property.py** - 30 мин
3. **test_serialization_roundtrip_property.py** - 30 мин
4. **test_overlap_new_model.py** - 1 час
5. **test_smoke.py** - 1 час
6. **test_career_matrix.py** - 45 мин

**Итого**: 4.5 часа работы → 100% P0-тестов работают

### Сценарий 4: "Как понять, что тест полезен?"

Смотрите колонку **"V2 Applicable"** в реестре:

- **Definitely** → точно полезное, мигрируем обязательно
- **Probably** → скорее всего полезное, мигрируем при возможности
- **Unclear** → непонятно, оценим позже
- **Not** → не полезное, архивируем

### Сценарий 5: "Нужна статистика для отчёта"

Откройте `test-migration-status.md`:

**Текущее состояние**:
- Всего тестов: **475**
- Ошибки импорта: **98** (20.6%)
- Загружаются: **377** (79.4%)

**По приоритетам**:
- P0: 28 файлов (~320 тестов) - **66% загружаются**
- P1: 67 файлов (~850 тестов) - не начато
- P2: 31 файл (~360 тестов) - не начато
- P3: 6 файлов (~80 тестов) - не начато

## Обновлённая инфраструктура

### Новые команды в Makefile

```bash
make test       # Только core v2 тесты (19 тестов) - базовый набор
make test-p0    # P0-тесты (24 файла, ~320 тестов) - расширенный набор
make test-all   # Все тесты (475 тестов) - полный набор
```

### Что изменилось

**Было**:
```makefile
make test  # → только 2 файла (test_domain_properties.py, test_v2_properties.py)
```

**Стало**:
```makefile
make test     # → те же 2 файла (стабильная база)
make test-p0  # → 24 файла P0-приоритета (комплексное покрытие)
```

## Практический workflow

### Ежедневная работа

1. **Убедитесь, что базовые тесты проходят**:
   ```bash
   make test
   ```

2. **Проверьте P0-тесты** (после миграции):
   ```bash
   make test-p0
   ```

### Миграция теста

1. Откройте `MIGRATION_QUICK_REFERENCE.md`
2. Следуйте чеклисту:
   - [ ] Обновить импорты на v2
   - [ ] Адаптировать ChunkConfig (32→8 параметров)
   - [ ] Обновить ссылки на стратегии (6→3)
   - [ ] Запустить: `pytest tests/path/to/test.py -v`
   - [ ] Исправить ошибки
   - [ ] Проверить, нет ли warnings
   - [ ] Добавить заметку в docstring
   - [ ] Закоммитить

### Когда нужна помощь

**Мигрируйте сами**, если:
- ✅ Простое обновление импортов
- ✅ Тест похож на уже мигрированные
- ✅ Property-based тест

**Запросите помощь**, если:
- ❌ Тест использует удалённые компоненты (OverlapManager и т.д.)
- ❌ Неясно, что тест проверяет
- ❌ Застряли >30 минут
- ❌ Нужны изменения в v2-реализации

## Метрики успеха

### Фаза 1 (текущий спринт)

| Метрика | Базовая линия | Цель | Статус |
|---------|--------------|------|--------|
| P0-тесты работают | 210/~320 (66%) | ~320/~320 (100%) | 🟡 66% |
| Ошибки импорта (P0) | 6 | 0 | 🔴 6 |
| Покрытие v2 | TBD | >70% | 🟡 TBD |

### Конечная цель

- **~1530 тестов** работают (P0+P1+P2)
- **<5 ошибок** импорта
- **>90% покрытие** кода v2
- **<15 минут** на полный прогон

## Частые вопросы

**Q: Что делать с тестами категории E?**  
A: Архивировать с документацией. См. Appendix D в `legacy-test-analysis.md`

**Q: Как узнать, какие стратегии удалены?**  
A: Legacy (6): code, list, table, structural, sentences, mixed  
   V2 (3): code_aware, structural, fallback  
   Удалены: list, table, sentences, mixed

**Q: Где найти маппинг параметров конфига?**  
A: `MIGRATION_QUICK_REFERENCE.md` → раздел "Config Parameter Mapping"

**Q: Как проверить прогресс?**  
A: `test-migration-status.md` → раздел "Progress Tracking"

## Следующие шаги

### Сейчас (этот спринт)
1. Мигрировать 6 P0-тестов (4.5 часа)
2. Запустить `make test-p0` - должно быть 100% success
3. Добавить `make test-p0` в CI

### Потом (2-3 спринта)
1. Мигрировать 67 P1-тестов
2. Достичь 85% покрытия v2
3. Создать скрипт автоматизации частых паттернов

### В будущем
1. Мигрировать P2-тесты (opportunistic)
2. Оценить P3-тесты
3. Архивировать категорию E
4. Удалить markdown_chunker_legacy

---

**Быстрый старт**: Откройте `docs/legacy-tests-analysis/README.md` - там навигация по всей документации!
