# Dify Plugin Debug Configuration
# 
# This file contains example configuration for debugging the plugin
# with a Dify instance. Copy this file to .env and fill in your values.
#
# IMPORTANT: Never commit .env to version control!
# The .env file should be in .gitignore

# Installation method for debugging
# Options: local, remote
INSTALL_METHOD=remote

# Remote installation configuration
# These settings are used when INSTALL_METHOD=remote
# Get your debug key from Dify UI: Settings > Plugin Debug

# Dify debug server host
REMOTE_INSTALL_HOST=https://debug.dify.ai

# Dify debug server port
REMOTE_INSTALL_PORT=5003

# Your unique debug key (get from Dify UI)
# Replace this with your actual debug key
REMOTE_INSTALL_KEY=your_debug_key_here

# Notes:
# 1. To get your debug key:
#    - Open Dify UI
#    - Go to Settings > Plugin Debug
#    - Copy the debug key shown
#
# 2. To use debug mode:
#    - Copy this file to .env
#    - Replace your_debug_key_here with your actual key
#    - Run: dify-plugin plugin run
#
# 3. Before packaging:
#    - Delete the .env file
#    - Only .env.example should be in the repository
